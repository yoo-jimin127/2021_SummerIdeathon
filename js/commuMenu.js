/*

const communityInNavMenu = document.querySelector(".communityInNavMenu");
const contentsInNavMenu = document.querySelector(".contentsInNavMenu");
const communitySubNav = document.querySelector(".communitySubNav");
const contentsSubNav = document.querySelector(".contentsSubNav");

communityInNavMenu.addEventListener("mouseover", () => {
  contentsSubNav.classList.replace("hovered", "notHovered");
  communitySubNav.classList.replace("notHovered", "hovered");
});
communitySubNav.addEventListener("mouseover", () => {
  contentsSubNav.classList.replace("hovered", "notHovered");
  communitySubNav.classList.replace("notHovered", "hovered");
});
communitySubNav.addEventListener("mouseout", () => {
  communitySubNav.classList.replace("hovered", "notHovered");
});

contentsInNavMenu.addEventListener("mouseover", () => {
  communitySubNav.classList.replace("hovered", "notHovered");
  contentsSubNav.classList.replace("notHovered", "hovered");
});
contentsSubNav.addEventListener("mouseover", () => {
  communitySubNav.classList.replace("hovered", "notHovered");
  contentsSubNav.classList.replace("notHovered", "hovered");
});
contentsSubNav.addEventListener("mouseout", () => {
  contentsSubNav.classList.replace("hovered", "notHovered");
});

*/